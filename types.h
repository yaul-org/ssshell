#ifndef TYPESS_H
#define TYPESS_H

struct env_pair;
typedef struct env_pair env_pair_t;

struct object;
typedef struct object object_t;

struct parser;
typedef struct parser parser_t;
struct parser_stream;
typedef struct parser_stream parser_stream_t;

struct command;
typedef struct command command_t;

#endif /* TYPESS_H */
